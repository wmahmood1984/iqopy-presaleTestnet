import Register from "@/components/register";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Register iQopy | Smart's Money Edge",
};
const index = () => {
   return (
      <Wrapper>
         <Register />
      </Wrapper>
   )
}

export default index