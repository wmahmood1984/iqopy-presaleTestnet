import Login from "@/components/login";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Login iQopy | Smart's Money Edge",
};
const index = () => {
   return (
      <Wrapper>
         <Login />
      </Wrapper>
   )
}

export default index