import ResetPassWord from "@/components/reset-password";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Reset PassWord iQopy | Smart's Money Edge",
};
const index = () => {
   return (
      <Wrapper>
         <ResetPassWord />
      </Wrapper>
   )
}

export default index